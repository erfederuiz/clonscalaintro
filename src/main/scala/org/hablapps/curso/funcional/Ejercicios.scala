package org.hablapps.curso.funcional

object Ejercicios extends App {

  // Ejercicio 1: Implementa `concatenar` (utiliza la siguiente signatura)

  // def concatenar(l1: Lista, l2: Lista): Lista

  // Ejercicio 2: Implementa `existe`.
  // - El tipo Boolean contiene los valores `true` y `false`
  // - El operador lógico "or" se representa mediante `b1 || b2`.

  // Ejercicio 3: Implementa el método `contiene` que se apoya en `existe`

  // Ejercicio 4: Implementa `tirarMientras` que elimina todos aquellos
  // elementos de la cabeza que cumplan con la condición, hasta que encuentre
  // uno que no lo haga, momento en el que se devolverá el resto de la lista
  // (elemento incluido)
  // - La estructura de control de flujo "if" se despliega con esta sintáxis:
  // `if(b) expr1 else expr2`
}
