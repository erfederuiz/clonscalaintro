package org.hablapps.curso.intro

object Ejercicios {

  object Ejercicio1 {
    // Arranca SBT mediante `sbt` o `sbt.bat` (Windows). Después, compila los
    // fuentes mediante la tarea `compile`. Finalmente, ejecuta el "Hola Mundo"
    // usando la tarea `run`.
  }

  object Ejercicio2 {
    // Ejecuta la tarea `~ run`. Después, modifica el texto que se imprime por
    // pantalla (por "Hola Mundo 2") y guarda el fichero. ¿Qué ha pasado en la
    // consola de SBT?
  }

  object Ejercicio3 {
    // Abre la REPL de Scala utilizando la tarea `console`. Evalua la expresión
    // `2 + 2`. Se creará un valor `res0` de tipo `Int`. Imprime dicho valor
    // utilizando `println`.
  }
}
